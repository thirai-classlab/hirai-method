# .claude/bash-whitelist.txt
#
# === Bash 制御の Single Source of Truth ===
#
# メインエージェントが Bash で実行できるコマンド prefix のホワイトリスト。
# 1 行 1 つの正規表現を書く。先頭一致 (^) を必須にすること。
# # で始まる行はコメント。空行は無視。
#
# ## SSoT ルール
# - Bash 個別 allow を `settings.json` / `settings.local.json` に重複追加しない
#   (重複は permission モデル分裂の原因。Wave 3-A で一本化済)
# - 追加は本ファイルへの 1 行追記のみ。`settings.json` 側は触らない
#
# ## 追加方法
# 1. `.claude/bash-whitelist-requests/YYYY-MM-DD-<slug>.md` を `REQUEST_TEMPLATE.md` に従い作成
# 2. user がレビュー → 本ファイルに 1 行追記
# 3. その瞬間から有効 (delegation-guard.sh が再読する)
#
# ## 理由
# 「メインは src/ tests/ scripts/ を直接さわらない (= サブエージェント委譲)」原則を
# Bash 経由で迂回されないようにするための allow-list。承認のないコマンドは
# delegation-guard.sh で block される。
#
# ## セクションマーカー (W1.2 で追加)
# 以下 2 つのセクションマーカー行は delegation-guard.sh が動的に読む:
#   `# === PATH-AWARE ===`     -> src/ tests/ scripts/ を引数に取りうるコマンド
#   `# === PATH-RESTRICTED ===` -> src/ tests/ scripts/ を引数に取ってはいけないコマンド
# delegation-guard.sh は PATH-AWARE セクションの正規表現の先頭トークンを抽出し、
# Bash 末尾の path-leak ガードで「これらは src/ paths を含んでよい」例外として扱う。
# セクションマーカーを変更/削除すると path-leak ガードが全コマンドを block しうるので注意。

# === PATH-AWARE ===
# (これらのコマンドは引数として src/ tests/ scripts/ を受け取りうる)

# git: 全 subcommand / global option 許可。以下 2 layer で delegation-guard.sh が常時 block:
#
#   1. git destructive deny: push --force / push -f / reset --hard / branch -D / clean -f /
#      checkout -- / restore --worktree|--source / stash drop|clear / tag -d|-f / reflog expire /
#      gc --prune=now
#      bypass: ECC_ALLOW_DESTRUCTIVE_GIT=1 (1 セッション)
#
#   2. protected branch push deny: main / stg を含む branch への push (明示 refspec + current
#      branch fallback の 2 段 check)
#      bypass: ECC_ALLOW_PROTECTED_BRANCH_PUSH=1 (1 セッション)
#
# 設計起源:
#   - 2026-05-18 user 指示「mainAgentでgitコマンドは基本的(破壊的変更以外)に実行可能」(destructive deny)
#   - 2026-05-18 user 指示「gitの許可はmainとstgと含むブランチに対するpush、破壊的変更以外に対してを許可」(protected branch deny)
^git( |$)

# パッケージマネージャ系の run/scripts 経路 (build/test/lint 等)
^npm run( |$)
^pnpm( |$)
^yarn( |$)

# プラットフォーム CLI
^vercel( |$)
^supabase( |$)

# GitHub CLI
^gh( |$)

# === PATH-RESTRICTED ===
# (これらのコマンドは src/ tests/ scripts/ を直接 inspect してはいけない)

# 軽い情報取得
^ls( |$)
^pwd( |$)
^date( |$)
^echo( |$)
^cat \.claude/
^cat \.gitmessage( |$)

# read-only 検索コマンド (task-50 grep-whitelist-add、2026-05-28)
# FleetView クライアントは Grep/Glob tool を非提供のため、bash 経由の検索手段を確保する。
# これらは PATH-RESTRICTED に置く (PATH-AWARE ではない) ため、src/ tests/ scripts/ を
# 直接 inspect する形 (例: `grep pat src/foo.ts` / `find src/ -name x`) は
# delegation-guard.sh 末尾の path-leak ガードで block され続ける (委譲原則を維持)。
# 申請: .claude/bash-whitelist-requests/2026-05-28-grep-find-rg.md
^grep( |$)
^find( |$)
^rg( |$)

# read-only filter コマンド (task-68 Step 3 delegation-guard-splitter-false-positive、2026-06-01)
# 背景: main の Bash を pipe (`| head`, `| tail`, `| wc`) で segment 分割 → 各 segment を
#   whitelist 照合する際、pipe 先の read-only filter が whitelist 不在で誤 BLOCK されていた
#   (例: `git push origin feat/x | tail -5` / `git log --oneline | wc -l` が segment `tail`/`wc`
#    不在で block)。本セッションでも複数回発火。
# 方針: read-only filter のみを PATH-RESTRICTED に追加 (= surgical whitelist 追加)。
#   - PATH-RESTRICTED 配置のため path-leak ガード対象外にならず、`head src/foo.ts` /
#     `cat src/x | head` 等 src/tests/scripts inspect は引き続き block (委譲原則維持)。
#   - 危険コマンド deny (git destructive / protected-branch push) は cmd 全体に対して
#     segment 非依存で先行判定するため不変 (例: `git push --force | head` は依然 block)。
#   - これらは破壊的 subcommand を持たない pure read-only filter のみに限定
#     (rm / mv / cp / dd / tee / xargs / sed -i 等の副作用系は意図的に除外)。
# 申請: .claude/bash-whitelist-requests/2026-06-01-readonly-filters.md
^head( |$)
^tail( |$)
^wc( |$)
^sort( |$)
^uniq( |$)
^nl( |$)
^cut( |$)
^tr( |$)
^column( |$)

# === 自己改善 L4: Continuous Learning v2.1 ===
# instinct-cli の管理操作（status / projects / evolve / promote / export / import / observe-analyze）
^python3 \.claude/skills/continuous-learning-v2/instinct-cli\.py( |$)

# === 自己改善 L1: Eval-Driven Dev ===
# 自前 eval grader 用（read-only な用途のみ。書き込みは Edit/Write で）
^cat \.claude/evals/

# === 監査 (W2.2) ===
# harness-audit スクリプトのみ許可（read-only）
^python3 \.claude/scripts/harness-audit\.py( |$)

# === タスク管理 helper (task-34) ===
# /new-task command が経路 B (batch planning) で list.md の 📝 → 🔲 update 動作を機械実装する helper。
# 規範: `.claude/rules/task-management.md` §plan-first 経路 B (採用 6 条 規範化、2026-05-25)
# 引数: update_or_append_task_row <id> <slug> <row_content> [<list_md>]
# 設計起源: docs/draft/list-md-plan-first-normative.md §3 P2、task-34 iter3 で 6 reviewer 並列検証
^bash \.claude/scripts/new-task-helper\.sh( |$)
