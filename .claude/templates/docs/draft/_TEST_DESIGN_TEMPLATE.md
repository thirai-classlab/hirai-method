# Test Design: <タスク名>

> このファイルは `/test-design <slug>` で `<slug>.test-design.md` として `docs/draft/` に生成される。
> 対応する設計 draft: `docs/draft/<slug>.md`
> 生成日: YYYY-MM-DD

## 目的

本タスクで「採用する/採用しない」テストを user が明示的に決定する。
不採用テストには **必ず理由** を書く。`/new-task` は本ファイルの未確認行 (☐) がある場合 block される (W4 hook で実装)。

## MECE テストカタログ (20 カテゴリ)

各行は ☑ (採用) か ☒ (不採用 + 理由) のいずれかで埋めること。
☐ (未確認) のままだとタスク化 block。

| # | カテゴリ | 観点 | 採用 (☑/☒) | 不採用理由 / 採用範囲メモ |
|---:|---|---|:---:|---|
| 1 | **単体テスト** | 関数/クラス単位の入出力、純粋関数の正当性 | ☐ | |
| 2 | **統合テスト** | 複数モジュール間の連携、DI 解決、middleware chain | ☐ | |
| 3 | **E2E テスト** | ユーザ操作シナリオ全体、Playwright 等 | ☐ | |
| 4 | **DB テスト** | migration / RLS / トランザクション / FK / index | ☐ | |
| 5 | **境界値テスト** | min/max/off-by-one/null/empty/最大長 | ☐ | |
| 6 | **異常系テスト** | 例外パス、catch ブロック、エラー伝播 | ☐ | |
| 7 | **回帰テスト** | 既存機能の挙動が変わっていないこと、過去 bug の再現防止 | ☐ | |
| 8 | **カバレッジ計測** | line / branch / function coverage、最低 80% 等 | ☐ | |
| 9 | **網羅性検証** | 全 if/switch ブランチ網羅、cyclomatic complexity 上の to-do | ☐ | |
| 10 | **完全性検証** | 仕様書の全要件にテスト紐付け、要件 ID とテスト ID の trace 表 | ☐ | |
| 11 | **性能テスト (レスポンスタイム)** | 95p / 99p latency、SLA 目標 (例: p95 < 200ms) | ☐ | |
| 12 | **負荷テスト** | 高 QPS / 大量データ / 長時間稼働 / メモリリーク | ☐ | |
| 13 | **セキュリティテスト** | 認可 / SQL injection / XSS / CSRF / secret 漏洩 | ☐ | |
| 14 | **互換性テスト** | ブラウザ / OS / API バージョン / DB バージョン | ☐ | |
| 15 | **アクセシビリティ** | WCAG 準拠、キーボード操作、スクリーンリーダ | ☐ | |
| 16 | **i18n テスト** | 多言語 / RTL / 長文字列 / 通貨フォーマット | ☐ | |
| 17 | **smoke テスト** | 本番デプロイ後の基本疎通、health check | ☐ | |
| 18 | **シナリオテスト** | ユーザストーリ全体、複数ステップの統合 flow | ☐ | |
| 19 | **chaos / 障害注入** | 依存サービス停止時の挙動、network partition、retry | ☐ | |
| 20 | **契約テスト** | API consumer/provider 間、schema 互換性、Pact 等 | ☐ | |

## 採用テストの実行戦略

採用 (☑) した各カテゴリについて、具体的な実装計画を以下に記載。

### TC-<カテゴリ番号>: <カテゴリ名>

- **対象**: <ファイル / モジュール>
- **ツール**: <vitest / jest / pytest / playwright / k6 / ...>
- **観点**: <具体的なテストケース 3-5 件>
- **完了条件**: <PASS 基準>

(採用カテゴリごとに繰り返し)

## 不採用テストの記録

不採用 (☒) は理由を以下のフォーマットで残す。後日 review でき、選定根拠が trace 可能。

### TC-<カテゴリ番号>: <カテゴリ名> — 不採用

- **理由カテゴリ**: `scope-excluded` / `not-applicable` / `existing-coverage` / `accepted-risk` のいずれか
- **詳細**: <なぜこのタスクでは不要か。誰がいつ判断したか>
- **再評価トリガー**: <将来何が起きたら再採用すべきか>

(不採用カテゴリごとに繰り返し)

## ユーザ承認

- [ ] 全 20 カテゴリの採用/不採用が決定済
- [ ] 不採用に理由が記載済
- [ ] 採用テストの実行戦略が具体化済

承認者: <user name>
承認日時: YYYY-MM-DD HH:MM

---

最終更新: YYYY-MM-DD
